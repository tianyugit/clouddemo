server:
  port: 8081
  servlet:
    path: /
#jdbc
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yun6
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: user-service
#mybatis
mybatis:
  configuration:
    type-aliases-package: com.sise.pojo
eureka:
  client:
    service-url:
        defaultZone: http://localhost:10086/eureka

  #  map-underscore-to-camel-case: true  #通用mapper已经帮忙解决，不需要这个了
   # mapper-locations: mapper/*.xml
#logging:
 # level:
   # com.sise: debug