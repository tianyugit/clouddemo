server:
  port: 10086
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  instance:  # 把ip固定
    prefer-ip-address: true
    ip-address:  127.0.0.1
spring:
  application:
    name: Eureka-server